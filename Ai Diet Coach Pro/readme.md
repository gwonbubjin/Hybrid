🥗 AI Diet Coach (AI 식단 코치)

Google Gemini 2.0 Flash와 Supabase를 활용한 지능형 식단 관리 PWA (Progressive Web App)

📖 프로젝트 개요 (Overview)

AI Diet Coach는 사용자가 섭취한 음식 사진을 찍거나 텍스트로 입력하면, Generative AI가 영양 성분을 정밀 분석하고 피드백을 제공하는 헬스케어 서비스입니다.

단순한 칼로리 기록을 넘어, 사용자의 신체 데이터(키, 체중, 나이, 활동량)를 기반으로 미플린-세인트 지어(Mifflin-St Jeor) 공식을 적용하여 과학적인 목표를 설정합니다. 또한, 축적된 데이터를 **시각화(Chart.js)**하여 사용자가 자신의 식습관과 체중 변화를 직관적으로 파악할 수 있도록 돕습니다.

✨ 핵심 기능 (Key Features)

1. 🤖 멀티모달 AI 식단 분석

이미지 인식: 음식 사진 촬영 시 AI가 음식 종류와 양을 인식하여 영양소(탄/단/지) 자동 분석.

자연어 처리: "닭가슴살 샐러드랑 아메리카노 마셨어"와 같은 자연어 입력 처리.

건강 피드백: 식단의 영양 균형 점수(0~100점) 산출 및 개선점, 칼로리 소모를 위한 추천 운동 제안.

2. 📊 데이터 기반 대시보드

실시간 트래킹: 일일 권장 칼로리 대비 섭취량(도넛 차트) 및 다량 영양소 비율(Progress Bar) 시각화.

True Black UI: 모바일 환경과 OLED 디스플레이에 최적화된 고대비 다크 모드 디자인 적용.

3. 📅 스마트 캘린더 & 히스토리

캘린더 뷰: 날짜별 데이터 기록 여부를 직관적인 인디케이터(Dot)로 표시.

상세 기록: 과거 날짜 선택 시 해당 일자의 섭취 리스트와 상세 영양 정보 제공.

4. 📈 고급 통계 분석

영양 밸런스 (Radar Chart): 탄수화물, 단백질, 지방, 칼로리 준수율, 건강 점수를 5각 레이더 차트로 분석.

체중 변화 (Area Chart): 체중 변화 추이를 그라데이션 영역 차트로 시각화하여 동기 부여.

5. 📱 PWA (Progressive Web App)

앱 설치 지원: manifest.json 및 service-worker.js 구현으로 모바일 홈 화면에 네이티브 앱처럼 설치 가능.

오프라인 대응: 네트워크 불안정 시에도 기본 UI 로드 지원.

🛠 기술 스택 (Tech Stack)

분류

기술

비고

Frontend

HTML5, CSS3, Vanilla JavaScript

ES6+ 문법, Flexbox/Grid 레이아웃

Backend

Supabase

Authentication, PostgreSQL Database

AI Model

Google Gemini 1.5 Flash

Multimodal(Text/Image) Generative AI

Library

Chart.js, FullCalendar

데이터 시각화 및 일정 관리

Deploy

GitHub Pages

정적 웹 호스팅

💾 데이터베이스 스키마 (Database Schema)

본 프로젝트는 **Supabase(PostgreSQL)**를 사용하며, 아래와 같은 테이블 구조를 가집니다.

1. users (사용자 프로필)

Column

Type

Description

id

uuid (PK)

Supabase Auth ID와 연동

height

numeric

키 (cm)

weight

numeric

체중 (kg)

age

int

나이

gender

text

성별 (male/female)

activity_level

numeric

활동 계수 (1.2 ~ 1.9)

daily_calorie_target

numeric

BMR 기반 자동 계산된 목표 칼로리

2. food_logs (식단 기록)

Column

Type

Description

id

bigint (PK)

고유 ID

user_id

uuid (FK)

작성자 ID

date

date

기록 날짜

food_name

text

음식 이름

calories

numeric

칼로리

nutrients

jsonb

탄/단/지 정보 ({"carbs": 20, ...})

health_score

int

AI가 부여한 건강 점수

burn_activity

text

추천 운동 멘트

🚀 설치 및 실행 가이드 (Getting Started)

1. 프로젝트 클론

git clone [https://github.com/your-username/ai-diet-coach.git](https://github.com/your-username/ai-diet-coach.git)
cd ai-diet-coach


2. API 키 설정 (app.js)

app.js 파일 최상단의 설정 변수에 본인의 키를 입력합니다.

const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_KEY = 'YOUR_SUPABASE_ANON_KEY';
const GEMINI_API_KEY = 'YOUR_GEMINI_API_KEY';


3. Supabase 테이블 생성

Supabase SQL Editor에서 아래 쿼리를 실행하여 테이블을 생성합니다.

-- 사용자 정보 테이블
create table public.users (
  id uuid references auth.users not null primary key,
  height numeric, weight numeric, age int, gender text,
  activity_level numeric, goal text, daily_calorie_target numeric
);

-- 식단 기록 테이블
create table public.food_logs (
  id bigint generated by default as identity primary key,
  user_id uuid references auth.users not null,
  date date not null,
  food_name text, calories numeric, nutrients jsonb,
  health_score int, burn_activity text,
  created_at timestamp with time zone default timezone('utc'::text, now())
);

-- RLS(Row Level Security) 정책 활성화 필요


4. 실행

VS Code의 Live Server를 사용하거나 로컬 웹 서버를 통해 index.html을 실행합니다.

📱 스크린샷 (Screenshots)

(여기에 실제 앱 구동 화면 스크린샷을 배치하세요. /images 폴더 등에 저장 후 링크)

메인 대시보드

AI 분석 화면

통계 화면

<img src="" alt="Dashboard" width="200">

<img src="" alt="AI Analysis" width="200">

<img src="" alt="Stats" width="200">

📄 라이선스

This project is licensed under the MIT License.
